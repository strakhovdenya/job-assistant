[project]
name = "job-assistant"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.33.0",
]

[tool.uv.workspace]
members = [
  "apps/backend",
  "apps/frontend",
  "packages/shared"
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
